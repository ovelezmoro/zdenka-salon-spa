import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'zdenka-salon-spa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
